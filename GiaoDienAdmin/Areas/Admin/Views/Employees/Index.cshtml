@* @model IEnumerable<GiaoDienAdmin.Models.Employee>


@{
    ViewData["Title"] = "Danh sách nhân viên";
}

<div class="container mt-4">

    <h2 class="text-center">Danh sách nhân viên</h2>
    <p class="text-center">
        <a asp-action="Create" class="btn btn-success btn-lg">Thêm nhân viên</a>
    </p>

    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Tên nhân viên</th>
                    <th>Email</th>
                    <th>Số điện thoại</th>
                    <th>Ngày sinh</th>
                    <th>Trạng thái</th>
                    <th>Hành động</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var employee in Model)
                {
                    <tr>
                        <td>@employee.Id</td>
                        <td>@employee.FullName</td>
                        <td>@employee.Email</td>
                        <td>@employee.PhoneNumber</td>
                        <td>@employee.DateOfBirth?.ToString("yyyy-MM-dd")</td>
                        <td>@(employee.IsActive ? "Hoạt động" : "Không hoạt động")</td>
                        <td class="d-flex justify-content-evenly ">
                            <div class="flex-fill m-1">
                                <a href="@Url.Action("Edit", "Employees", new { id = employee.Id })" class="btn btn-warning btn-sm w-100; h-100">Sửa</a>
                            </div>
                            <div class="flex-fill m-1">
                                <a href="@Url.Action("Details", "Employees", new { id = employee.Id })" class="btn btn-info btn-sm w-100; h-100">Chi tiết</a>
                            </div>
                            <div class="flex-fill m-1">
                                <a href="@Url.Action("Delete", "Employees", new { id = employee.Id })" class="btn btn-danger btn-sm w-100; h-100">Xóa</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<style>
    /* Thêm thanh cuộn cho toàn bộ bảng */
    .table-responsive {
        max-height: 600px; /* Giới hạn chiều cao của bảng */

        overflow-y: auto; /* Cho phép cuộn dọc khi dữ liệu vượt quá chiều cao */
        overflow-x: auto; /* Cho phép cuộn ngang khi bảng rộng */
    }

    /* Điều chỉnh padding và viền cho bảng */
    .table th, .table td {
        vertical-align: middle;
        padding: 10px;
    }

    /* Kiểu ảnh khi không có hình */

</style>
 *@


 @model IEnumerable<GiaoDienAdmin.Models.Employee>


@{
    ViewData["Title"] = "Danh sách nhân viên";
}

<div class="container mt-4">

    <h2 class="text-center">Danh sách nhân viên</h2>
    <p class="text-center">
        <a asp-action="Create" class="btn btn-success btn-lg">Thêm nhân viên</a>
    </p>

    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Tên nhân viên</th>
                    <th>Email</th>
                    <th>Số điện thoại</th>
                    <th>Ngày sinh</th>
                    <th>Trạng thái</th>
                    <th>Hành động</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var employee in Model)
                {
                    <tr>
                        <td>@employee.Id</td>
                        <td>@employee.FullName</td>
                        <td>@employee.Email</td>
                        <td>@employee.PhoneNumber</td>
                        <td>@employee.DateOfBirth?.ToString("yyyy-MM-dd")</td>
                        <td>@(employee.IsActive ? "Hoạt động" : "Không hoạt động")</td>
                        <td class="d-flex justify-content-evenly ">
                            <div class="flex-fill m-1">
                                <a href="@Url.Action("Edit", "Employees", new { id = employee.Id })" class="btn btn-warning btn-sm w-100; h-100">Sửa</a>
                            </div>
                            <div class="flex-fill m-1">
                                <a href="@Url.Action("Details", "Employees", new { id = employee.Id })" class="btn btn-info btn-sm w-100; h-100">Chi tiết</a>
                            </div>
                            <div class="flex-fill m-1">
                                <a href="@Url.Action("Delete", "Employees", new { id = employee.Id })" class="btn btn-danger btn-sm w-100; h-100">Xóa</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<style>
    /* Thêm thanh cuộn cho toàn bộ bảng */
    .table-responsive {
        max-height: 600px; /* Giới hạn chiều cao của bảng */

        overflow-y: auto; /* Cho phép cuộn dọc khi dữ liệu vượt quá chiều cao */
        overflow-x: auto; /* Cho phép cuộn ngang khi bảng rộng */
    }

    /* Điều chỉnh padding và viền cho bảng */
    .table th, .table td {
        vertical-align: middle;
        padding: 10px;
    }

    /* Kiểu ảnh khi không có hình */

</style>
