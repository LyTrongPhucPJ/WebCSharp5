@* @model GiaoDienAdmin.Models.ProductCategories

@{
    ViewData["Title"] = "Xóa danh mục sản phẩm";
}

@section DeleteProductCategories {

    <div class="container mt-4">
        <div class="table-responsive">
            <!-- Form trong container với viền xung quanh -->
            <div class="row justify-content-center">
                <div class="col-md-8">
                    <div class="border p-4 rounded shadow-sm">
                        @if (!string.IsNullOrEmpty(Model.Name))
                        {
                            var fullName = $"{Model.Name}";
                            <h3 class="text-center text-danger">Bạn có chắc chắn muốn xóa danh mục @fullName không?</h3>
                        }
                        <hr />
                        <form asp-action="Delete" method="post" enctype="multipart/form-data">
                            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                            <!-- ID sản phẩm -->
                            <input type="hidden" asp-for="Id" />



                            <div class="form-group d-flex">
                                <h5 class="control-label mx-5">Tên danh mục</h5>
                                <div class="" style="margin: 3px;">
                                    @Model.Name
                                </div>
                            </div>




                            <div class="form-group text-center my-2">
                                <a asp-action="Index" class="btn btn-secondary btn-lg ml-2">Quay lại</a>
                                <input type="submit" value="Xóa" class="btn btn-danger btn-lg" />

                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

}

<style>
    /* Thêm thanh cuộn cho toàn bộ bảng */
    .table-responsive {
        max-height: 600px; /* Giới hạn chiều cao của bảng */

        overflow-y: auto; /* Cho phép cuộn dọc khi dữ liệu vượt quá chiều cao */
        overflow-x: auto; /* Cho phép cuộn ngang khi bảng rộng */
    }

</style>
<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
 *@



@model GiaoDienAdmin.Models.ProductCategories

@{
    ViewData["Title"] = "Xóa danh mục sản phẩm";
}

@section DeleteProductCategories {

    <div class="container mt-4">
        <div class="table-bordered">
            <!-- Form trong container với viền xung quanh -->
            <div class="row justify-content-center">
                <div class="col-md-8">
                    <div class="border p-4 rounded shadow-sm" style="max-width: 600px; margin: 0 auto;">
                        @if (!string.IsNullOrEmpty(Model.Name))
                        {
                            var fullName = $"{Model.Name}";
                            <h3 class="text-center text-danger">Bạn có chắc chắn muốn xóa danh mục @fullName không?</h3>
                        }
                        <hr />
                        <form asp-action="Delete" method="post" enctype="multipart/form-data">
                            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                            <!-- ID sản phẩm -->
                            <input type="hidden" asp-for="Id" />

                            <div class="form-group d-flex">
                                <h5 class="control-label mx-5">Tên danh mục</h5>
                                <div class="" style="margin: 3px;">
                                    @Model.Name
                                </div>
                            </div>

                            <div class="form-group text-center my-2">
                                <a asp-action="Index" class="btn btn-secondary btn-lg ml-2">Quay lại</a>
                                <input type="submit" value="Xóa" class="btn btn-danger btn-lg" />
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

}

<style>
    /* Căn giữa container và giới hạn chiều rộng */
    .container {
        max-width: 100%;
        padding-left: 15px;
        padding-right: 15px;
    }

    /* Căn giữa card và giới hạn chiều rộng */
    .card {
        max-width: 600px; /* Giới hạn chiều rộng của card */
        margin: 0 auto; /* Căn giữa card */
    }
</style>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
